### Arithmetic
#
# + - * / translate to same ops in python
# NOTE: `from __future__ import division` should be used to make / behave the same
#
# quot = //
# mod  = %
# TODO: rem (like `mod` but slightly different)

def inc(x):
    return x + 1

def dec(x):
    return x - 1

# max, min - builtins


### Compare
#
# = == not= < > <= >= roughly the same in python
# compare = cmp


### Bitwise, Cast, Test, Random, BigDecimal and Unchecked - skipped for now
